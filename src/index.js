import React from "react";
import ReactDOM  from "react-dom/client";


const root =
	ReactDOM.createRoot(document.getElementById('main'));
const element = <h1 id="title">Welcome too react</h1>;
root.render(element);